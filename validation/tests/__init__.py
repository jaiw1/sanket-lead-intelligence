"""Tests for the pre-registration contract itself.

These live under `validation/tests/` rather than the repo-level `tests/` so the
validation package stays self-contained and does not collide with the product
test suite.

    pytest validation/tests            # or
    python3 -m validation.tests.test_criteria
"""
